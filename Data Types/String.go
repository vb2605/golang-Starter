package main

import "fmt"

func main() {
	var s string
	s = "Let's learn Go"
	fmt.Println(s)
	fmt.Println(len(s)) // The len built-in function returns the length of v, according to its type: Array: the number of elements in v.
	fmt.Println()
}
